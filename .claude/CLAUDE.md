@../.codex/AGENTS.md
@RTK.md

## Editing Typescript

All `.ts`, `.tsx`, `.mts`, `cts`, `.js`, `.jsx`, `.mjs`, & `.cjs` files may only
be edited by the `typescript-engineer` agent. The main agent is forbidden from
making direct edits to these files.
